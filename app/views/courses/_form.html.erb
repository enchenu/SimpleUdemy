<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :image, id: :course_image, placeholder: "Image" %>
  </div>

  <div class="field">
    <%= form.text_field :video, id: :course_video, placeholder: "Video" %>
  </div>

  <div class="field">
    <%= form.text_field :title, id: :course_title, placeholder: "Title" %>
  </div>

  <div class="field">
    <%= form.number_field :star, id: :course_star, placeholder: "Star" %>
  </div>

  <div class="field">
    <%= form.text_area :description, id: :course_description, placeholder: "Description", length: "800", class: "materialize-textarea" %>
  </div>

  <div class="field">
    <%= form.check_box :public, id: :public, class: "filled-in" %>
    <label for="public">Public</label>
  </div>

  <button class="btn", type="submit">Submit</button>
  <%= link_to "Go to all courses", courses_path, class: "btn" %>
<% end %>
